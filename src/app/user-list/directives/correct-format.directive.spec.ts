import { CorrectFormatValidator } from './correct-format.directive';

describe('CorrectFormatValidator', () => {
  it('should create an instance', () => {
    const directive = new CorrectFormatValidator();
    expect(directive).toBeTruthy();
  });
});
